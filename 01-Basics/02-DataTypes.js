"use strict"; // If this line added in your code, treat all JS code as newer version.

// alert( 3 + 3); // we are using node.js environment, not the browser.

/*
    // Not this way.
    console.log(3 
    +
    3); // Code readability should be high, if you code.
    
    // This way.
    console.log(3 + 3);
*/

//console.log("Hitesh");

let name = "hitesh";
let age = 18;
let isLoggedIn = false;
let state = null;
let pincode;

// Primitive Data Types.

// number => 2 (to power) 53.
// bigint.
// string => " "/' '.
// boolean => (true/false) value.
// null => null is used to explicitly denote that a variable holds no value or an empty value.
// undefined => undefined usually means that a variable has been declared but not assigned a value. 
// symbol => unique value.


// Object
//console.log(typeof undefined); // Type is undefined.
//console.log(typeof null); // Type is object.
